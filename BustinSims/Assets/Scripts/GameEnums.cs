
public enum AnchorState {
	None,
	Active,
	Debug
}

public enum NodeState {
	Untested,
	Open,
	Closed
}

public enum WallFace {
	Undefined,
	North,
	East,
	South,
	West
}

public enum AnchorPosition {
	TopRight,
	TopLeft,
	BottomRight,
	BottomLeft
}